{
  "name": "coffee-estate-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npm run build"
  },
  "devDependencies": {
    "wrangler": "^3.114.17"
  }
}