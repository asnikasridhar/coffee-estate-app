Cloudflare Pages Functions placeholder.

For local testing, use the Express API in server/src/index.js.
For Cloudflare D1 deployment, convert those handlers to Pages Functions using env.DB.prepare(...).
